<div class="mb-4">
    <label for="{{$nombre}}" class="block mb-2 font-bold">{{$etiqueta}}</label>
    <textarea name="{{$nombre}}" id="{{$nombre}}" class="w-full px-3 py-2 border-2 border-black rounded-md" rows="3">{{$slot}}</textarea>
</div>